<h1>Edit <%= @kitten.name %></h2>

<%= render 'kitten_form' %>

<p><%= link_to "Delete #{@kitten.name}", kitten_path(@kitten), method: :delete, data: { confirm: 'Are you certain you want to delete this?' } %></p>
